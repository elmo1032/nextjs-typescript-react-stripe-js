// Set the currency to be used for transactions. In this case, it is US Dollars (USD).
export const CURRENCY = 'usd';

// Set the minimum amount limit for transactions. This value is in decimal form,
// which is suitable for currencies with decimal values such as USD.
export const MIN_AMOUNT = 10.0;

// Set the maximum amount limit for transactions. This value is also in decimal form.
export const MAX_AMOUNT = 5000.0;

// Set the amount step for transactions. This value is also in decimal form.
// It represents the amount by which the transaction amount will increase or decrease.
export const AMOUNT_STEP = 5.0;
