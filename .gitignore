# This is a list of file and directory patterns to be ignored by Git

# These files and directories are specific to the macOS operating system
# and can be safely ignored
.DS_Store

# This directory contains settings and cache data for Visual Studio Code
# and can be safely ignored
.vscode

# The following files and directories are generated by the Node.js package manager
# (npm) and can be safely ignored
node_modules/

# These files are generated by the TypeScript compiler and can be safely ignored
dist

# The following files and directories are generated by Next.js and can be safely ignored
.next
.vercel

# These files contain environment variables specific to local development
# and can be safely ignored
.env*.local
